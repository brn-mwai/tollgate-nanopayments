import { PageStub } from "@/components/page-stub";

export default function WithdrawalsPage() {
  return (
    <PageStub
      title="Withdrawals"
      subtitle="Off-ramp USDC via Circle CCTP or direct transfer on Arc"
    />
  );
}
