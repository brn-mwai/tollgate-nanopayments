import { PageStub } from "@/components/page-stub";

export default function EventsPage() {
  return (
    <PageStub
      title="Events"
      subtitle="Every 402, payment, cache hit, and webhook indexed by site + agent"
    />
  );
}
