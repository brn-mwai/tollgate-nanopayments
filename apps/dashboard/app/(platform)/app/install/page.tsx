import { PageStub } from "@/components/page-stub";

export default function InstallPage() {
  return (
    <PageStub
      title="Install the SDK"
      subtitle="One line of middleware. Start charging AI bots per request."
    />
  );
}
