import { PageStub } from "@/components/page-stub";

export default function SitesPage() {
  return <PageStub title="Sites" subtitle="Protected domains, per-path pricing, API keys, verification" />;
}
